# 🔑 Quick Setup Guide - API Key Configuration

## ⚡ ไฟล์ .env ถูกสร้างแล้ว!

ตอนนี้คุณต้องทำเพียง 2 ขั้นตอน:

---

## Step 1: รับ Gemini API Key (ฟรี!)

1. เปิด: https://makersuite.google.com/app/apikey
2. Login ด้วย Google Account
3. คลิก **"Create API Key"**
4. **Copy** API key ที่ได้ (จะขึ้นต้นด้วย `AIzaSy...`)

---

## Step 2: แก้ไขไฟล์ .env

**เปิดไฟล์ `.env`** ในโฟลเดอร์ root แล้วแก้ไข:

```env
VITE_GEMINI_API_KEY=YOUR_API_KEY_HERE
```

เปลี่ยนเป็น:

```env
VITE_GEMINI_API_KEY=AIzaSyD-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

(ใส่ API key จริงที่คุณ copy มา)

---

## Step 3: Restart Server

กลับมาที่ terminal แล้วรัน:

```bash
npm run dev
```

---

## ✅ ทดสอบ

หลัง restart server:

1. เปิดเว็บ: http://localhost:5173/
2. Login เข้าระบบ
3. **ทดสอบ AI Features**:
   - คลิกปุ่ม **💡 AI** ข้างๆ หนังสือ → ควรเห็นข้อมูลจาก AI
   - ไปเมนู **⭐ Recommendations** → กด "Get AI Recommendations"

---

## 🐛 หากยังไม่ทำงาน

### ตรวจสอบ:

1. **API Key ถูกต้องหรือไม่?**
   - ต้องขึ้นต้นด้วย `AIzaSy`
   - ไม่มีช่องว่าง
   - ไม่มี quotes ครอบ

2. **ไฟล์ .env อยู่ที่ถูกต้องหรือไม่?**
   ```
   frontend_book_store_712/
   ├── .env          ← ต้องอยู่ตรงนี้!
   ├── package.json
   ├── vite.config.js
   └── src/
   ```

3. **Restart server แล้วหรือยัง?**
   - ต้อง restart ทุกครั้งที่แก้ไข .env
   - กด Ctrl+C แล้ว `npm run dev` ใหม่

---

## 💡 วิธีอื่น (ถ้า .env ไม่ทำงาน)

**ใส่ API key ใน code โดยตรง:**

### แก้ไข `src/components/GeminiBookDetails.jsx` บรรทัด 10:

```javascript
// เปลี่ยนจาก:
const GEMINI_API_KEY = import.meta.env.VITE_GEMINI_API_KEY;

// เป็น:
const GEMINI_API_KEY = 'AIzaSyD-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx';
```

### แก้ไข `src/BookRecommendationPage.jsx` บรรทัด 10:

```javascript
const GEMINI_API_KEY = 'AIzaSyD-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx';
```

⚠️ **หมายเหตุ**: วิธีนี้ใช้ได้แต่ไม่ควร commit ขึ้น Git

---

## 📝 ตัวอย่างไฟล์ .env ที่ถูกต้อง

```env
# Gemini AI Configuration
VITE_GEMINI_API_KEY=AIzaSyDaBcD123456789ExampleKeyHere-xxxxx

# หมายเหตุ:
# - ไม่ต้องใส่ quotes
# - ไม่มีช่องว่างรอบๆ เครื่องหมาย =
# - ขึ้นต้นด้วย VITE_ เสมอ (สำหรับ Vite)
```

---

## 🎯 สถานะปัจจุบัน

✅ ไฟล์ `.env` ถูกสร้างแล้ว  
⏳ รอ: ใส่ API key จริง  
⏳ รอ: Restart server  
⏳ รอ: ทดสอบ AI features  

---

**ทำตามขั้นตอนแล้วบอกผลด้วยนะครับ! 🚀**
